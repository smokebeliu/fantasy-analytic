# Base URL of the Fantasy Analytics FastAPI backend. The Next.js server proxies
# every browser request from /api/backend/* to this host, so it only needs to be
# reachable from the Next.js process (not from the browser).
BACKEND_URL=http://127.0.0.1:8000
